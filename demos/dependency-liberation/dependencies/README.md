# Supplied dependencies

- `invoice/`: Excel event and Outlook/PDF workflow source.
- `postgresql/`: direct connection and scheduled refresh source.
- `install_windows.ps1`: evidence of Windows installation assumptions.

The installer and source modules are static migration inputs and must not be
run on the host.
