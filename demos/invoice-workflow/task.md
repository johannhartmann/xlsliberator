# Task

Migrate `source/Invoice_Generator.xlsm` to LibreOffice `26.2.4.2`. Replace the
four Excel UserForms with native LibreOffice dialogs or controls, preserve
customer CRUD and worksheet search events, produce PDF and spreadsheet invoice
artifacts, and replace Outlook COM with a portable message artifact that never
sends mail automatically.

All output paths must stay inside the granted episode artifact directory.
