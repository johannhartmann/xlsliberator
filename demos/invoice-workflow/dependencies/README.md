# Supplied dependencies

`forms/` contains the exported UserForm source/resources. `modules/` contains
the invoice module and worksheet class. These files expose the event and Office
dependencies required for a complete replacement; they are not executed on the
host.
