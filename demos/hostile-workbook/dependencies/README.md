# Supplied dependencies

The `.bas` and text files are synthetic adversarial inputs for static analysis
and the bounded security harness. They are intentionally not embedded in the
XLSX and must never be launched by a host or office startup event.
