# Supplied dependencies

The embedded VBA/OLE streams are the dependency bundle for this self-contained
legacy workbook. This marker makes that scope explicit; no external executable
is required or supplied.
