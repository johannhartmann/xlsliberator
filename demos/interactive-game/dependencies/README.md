# Supplied dependencies

The bundle contains the exported VBA modules and document classes used to
review event/state behavior, plus the two sound assets referenced by the
Windows implementation. They are source inputs, not host-executable tools.
