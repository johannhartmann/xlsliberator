# Supplied dependencies

The workbook's embedded VBA, chart, VML drawing, comments, styles, and
calculation-chain parts are the supplied dependency bundle. No external binary
is needed; each embedded part still requires an explicit migration disposition.
