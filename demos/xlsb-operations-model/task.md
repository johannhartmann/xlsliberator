# Task

Inventory and migrate `source/OperationsModel.xlsb` to LibreOffice `26.2.4.2`.
Account for worksheets, formulas, chart series, comments, drawings, styles,
calculation chain, and VBA. Minimize any generic XLSB parser failure into
`tests/corpus/regressions/`, then verify visible chart/comment behavior and
save/reopen stability.
