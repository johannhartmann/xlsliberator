# XLL/UDF replacement

The BIFF8 source workbook contains an external-function surface. The dependency
bundle is a real MIT XLL UDF implementation written in twinBASIC. The migration
must replace the native Windows XLL with a LibreOffice extension or Python/UNO
add-in and prove formula behavior after installation and reopen.
