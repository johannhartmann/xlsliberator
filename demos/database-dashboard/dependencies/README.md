# Supplied dependencies

`vba/` contains the original PostgreSQL connection, refresh, query, and error
modules. `sql/` contains a generated sample schema and data. No secrets,
database binary, or driver is bundled.
