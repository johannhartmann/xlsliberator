# Task

Turn `source/DatabaseDashboard.xlsx` and the supplied database modules into a
LibreOffice `26.2.4.2` dashboard. Replace ADODB/ODBC assumptions with a portable
adapter, load the bundled public schema and rows, connect a refresh control,
recalculate dashboard formulas, and preserve explicit unavailable behavior when
the database capability is not granted.

Queries must be parameterized and evidence must redact connection credentials.
