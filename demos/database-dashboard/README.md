# Database dashboard migration

The checked-in workbook is a deterministic, redistributable dashboard seed.
The supplied MIT VBA/SQL bundle represents the direct PostgreSQL/ODBC workflow
to liberate. The target must use a granted SQLite or PostgreSQL fixture instead
of ADODB, Access, or embedded credentials.

This episode tests record-level results and parameterization, not merely target
file creation.
