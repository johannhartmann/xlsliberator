# Composed corpus

Cross-feature and complete-workflow cases are indexed by
`tests/corpus/manifests/episodes.json` and stored under `demos/`. This directory
is reserved for minimized composed fixtures promoted from episode failures.
