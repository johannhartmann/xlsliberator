# Security corpus

Security cases are synthetic, safe, and bounded. Active source snippets live in
`tests/fixtures/security/`; the hostile episode combines them with an inert
workbook and public denial scenarios. No fixture may require host execution,
outbound network access, or unbounded resource use.
