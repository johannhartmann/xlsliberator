# Minimized regressions

Every generic parser, migration, execution, or review failure must be reduced to
a stable fixture here (or an existing format-specific fixture directory) with a
normalized failure signature and a link to the originating episode. A generic
failure is not closed merely by adding a special case to one demo.
